from setuptools import setup, find_packages

# """
setup(
	name="diutil", 
	version="0.1.0",
	description="just toolbox",
	packages=find_packages(),
	install_requires=[
		# "dependency1",
		# "dependency2",
		"pandas",
		"GitPython",
		"zope.datetime",
		"DateTime",
	],
)
# """